import React from 'react';
import Grid from '@mui/material/Grid';
import CommonButton from '../../componans/commom/CommonButton';

const Authentication = () => {
  return (
    <Grid item xs={8}>
        <CommonButton>Text</CommonButton>
    </Grid>
  )
}

export default Authentication
